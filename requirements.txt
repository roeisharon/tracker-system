# Runtime dependencies.
#
# IMPORTANT: use opencv-contrib-python (NOT opencv-python-headless). The contrib
# build provides the classical trackers (CSRT/KCF/MOSSE) and HighGUI windows the
# system depends on. Installing a headless build silently removes them.
#
# Pins are filled in from a verified clean install; see README for setup.
opencv-contrib-python==5.0.0.93
numpy==2.5.0
PyYAML==6.0.3
